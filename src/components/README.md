# Vue component directory

Add your Vue components here. 

* Provide a separate subfolder for each component. 
* The component folder **must** be named exactly as the defined component name.
* Split template (ComponentName.vue) and script (ComponentName.mjs). Single file components are not supported.  
